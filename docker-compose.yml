version: "3.8"
services:
  snaketestapp:
    image: schbros/chatappsonarpipe:latest
    build: .
    container_name: chatappzap
    ports: 
      - "80:3000"
    environment:
      - PORT=3000
    volumes:
      - ./:/app
    
